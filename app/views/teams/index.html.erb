<h1><%= t("teams.listing") %></h1>

<table class="table">
  <tr>
    <th><%= t("activerecord.attributes.team.name") %></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @teams.each do |team| %>
  <tr>
    <td><%= team.name %></td>
    <td><%= link_to t('show'), team %></td>
    <td><%= link_to t('edit'), edit_team_path(team) %></td>
    <td><%= link_to t('destroy'), team, method: :delete, data: { confirm: t('are-you-sure') } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to t("new"), new_team_path %>
