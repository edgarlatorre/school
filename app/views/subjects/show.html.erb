<p id="notice"><%= notice %></p>

<p>
  <b><%= t("activerecord.attributes.subject.name") %>:</b>
  <%= @subject.name %>
</p>

<p>
  <b><%= t("activerecord.attributes.subject.description") %>:</b>
  <%= @subject.description %>
</p>


<%= link_to t('edit'), edit_subject_path(@subject) %> |
<%= link_to t('back'), subjects_path %>
